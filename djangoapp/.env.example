# This file is a template. The setup script will generate the actual .env file.
# It will be populated with the DATABASE_URL connection string based on your input.

DATABASE_URL="postgres://user:password@host:port/dbname"